import Foundation

enum TrainLoggingDeferred {
    static let note = """
    M2.1 deferred polish (do not implement until a later milestone):
    exercise photos, plate calculator, RPE prominence, inline workout title edit, exercise reorder UX polish.
    """
}
